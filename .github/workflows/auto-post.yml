name: AI記事自動投稿

on:
  schedule:
    # 毎日午前10時（日本時間 ）に実行（UTCでは午前1時）
    - cron: '0 1 * * *'
  workflow_dispatch:  # 手動実行も可能

jobs:
  auto-post:
    runs-on: ubuntu-latest
    
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Pythonセットアップ
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: 依存パッケージをインストール
        run: |
          pip install -r scripts/requirements.txt
      
      - name: AI記事を生成してWordPressに投稿
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WP_URL: ${{ secrets.WP_URL }}
          WP_USERNAME: ${{ secrets.WP_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
        run: |
          cd scripts
          python3 auto-post.py
      
      - name: 変更をコミット
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add scripts/keywords.json scripts/article_*.json
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 AI記事自動投稿: $(date +'%Y-%m-%d %H:%M:%S')"
      
      - name: 変更をプッシュ
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
